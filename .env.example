# Copia este archivo a .env y completa los valores.
# Credenciales OAuth de usuario (usa solo una de las dos opciones)
GOOGLE_OAUTH_CLIENT_JSON=""
GOOGLE_OAUTH_CLIENT_FILE="./oauth_client.json"
# Ruta donde se guardará el token refrescable (se crea automáticamente)
GOOGLE_OAUTH_TOKEN_FILE=".streamlit/oauth_token.json"

# Google APIs
GOOGLE_SHEETS_SPREADSHEET_ID=""
GOOGLE_CALENDAR_ID="primary"

# Gmail (usa una contraseña de aplicación)
GMAIL_USER=""
GMAIL_APP_PASSWORD=""

# Zona horaria
TZ="America/Bogota"
